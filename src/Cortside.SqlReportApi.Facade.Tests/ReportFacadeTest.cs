namespace Cortside.SqlReportApi.Facade.Tests {
    public class ReportFacadeTest {
        //[Fact]
        //public async Task ShouldGetCustomerAsync() {
        //    // arrange
        //    var uow = new Mock<IUnitOfWork>();
        //    var customerService = new Mock<ISqlReportService>();
        //    var facade = new ReportFacade(uow.Object, customerService.Object, new Mappers.ReportMapper());
        //    var customerResourceId = Guid.NewGuid();
        //    customerService.Setup(x => x.GetCustomerAsync(customerResourceId)).ReturnsAsync(new Domain.Entities.Customer("elmer", "fudd", "elmer.fudd@gmail.org"));

        //    // act
        //    var customer = await facade.GetReportAsync(customerResourceId);

        //    // assert
        //    Assert.NotNull(customer);
        //}
    }
}
